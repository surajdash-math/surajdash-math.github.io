<footer class="site-footer">
  <p>© Suraj Dash. Last modified: <time datetime="2026-09-23">September 23, 2026</time>.</p>
</footer>

<style>
  .site-footer {
    position: absolute;
    left: 50%;
    width: 100vw;
    bottom: 0;
    transform: translateX(-50%);
    padding: 11px 18px;
    border: 1px solid color-mix(in srgb, var(--footer-text, var(--muted, #53636e)) 16%, transparent);
    color: var(--footer-text, var(--muted, #53636e));
    background: var(
      --footer-bg,
      color-mix(in srgb, var(--surface, #fafcfd) 86%, var(--heading, #304957))
    );
    box-shadow: 0 -8px 24px color-mix(in srgb, var(--shadow, rgba(36, 49, 58, 0.09)) 70%, transparent);
    opacity: 0.7;
    text-align: center;
    pointer-events: none;
  }

  .site-footer p {
    margin: 0;
    color: inherit;
    font-family: "DM Sans", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    font-size: 0.7rem;
    font-weight: 400;
    line-height: 1.4;
  }

  :global(html[data-theme="light"]) .site-footer {
    border-color: color-mix(in srgb, var(--heading, #304957) 22%, transparent);
    color: var(--heading, #304957);
    opacity: 0.9;
  }

  :global(html[data-theme="dark"]) .site-footer {
    border-color: color-mix(in srgb, var(--footer-text) 14%, transparent);
  }

  @media (max-width: 680px) {
    .site-footer {
      padding-inline: 12px;
    }

    .site-footer p {
      font-size: 0.62rem;
    }
  }
</style>
